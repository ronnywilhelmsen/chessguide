<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      ChessGuide Android.
      Camera permission is used ONLY by the Camera Sandbox (CameraX preview +
      ImageAnalysis frame-metadata sandbox). No image is captured or persisted,
      no CV/ML runs, and nothing is sent over the network.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:label="ChessGuide Android"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChessGuide">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
